import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads successfully', (WidgetTester tester) async {
    // Placeholder test — will be expanded with feature-specific tests
    expect(true, isTrue);
  });
}
